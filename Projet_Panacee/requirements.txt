# Dependances Projet Panacee v3
#
# Installation (dans l'ordre) :
#   1) PyTorch :
#        GPU CUDA 12.1 : pip install torch --index-url https://download.pytorch.org/whl/cu121
#        CPU           : pip install torch --index-url https://download.pytorch.org/whl/cpu
#   2) PyTorch Geometric : pip install torch-geometric
#   3) Le reste          : pip install -r requirements.txt
#
# NB Kaggle : torch + CUDA sont deja preinstalles, ne pas reinstaller torch.

# Core
torch>=2.0.0
torch-geometric>=2.3.0
numpy>=1.24.0
scipy>=1.10.0

# Chimie moleculaire (paquet officiel ; PAS rdkit-pypi qui est deprecie)
rdkit>=2023.3.1

# ML / metriques
scikit-learn>=1.3.0
pandas>=2.0.0

# Datasets toxicite / multi-proprietes (Tox21, ESOL, Lipo, BBBP, ClinTox, HIV)
deepchem>=2.7.0

# Utilitaires
tqdm>=4.65.0
psutil>=5.9.0
packaging>=23.0
python-dotenv>=1.0.0

# Visualisation (optionnel)
matplotlib>=3.7.0

# Recherche web / connaissances (optionnel, Phase 3 avancee)
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
biopython>=1.81
