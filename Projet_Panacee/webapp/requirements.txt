# Backend du tableau de bord web Panacée (REST + SSE temps réel)
# Léger : pas de FastAPI, on s'appuie sur Starlette + uvicorn.
starlette>=0.40
uvicorn>=0.30

# Pour l'onglet "Métriques cliniques" (évaluation d'un checkpoint), le projet
# principal fournit déjà : torch, torch-geometric, rdkit, deepchem, scikit-learn,
# numpy, pandas. Voir Projet_Panacee/requirements.txt.

# Chatbot — mode Claude (OPTIONNEL ; sans lui, l'assistant local fonctionne) :
#   pip install anthropic   + variable d'env ANTHROPIC_API_KEY
# anthropic>=0.40

# Dépendances de DEV uniquement (tests) :
#   httpx>=0.27   # requis par starlette.testclient.TestClient
